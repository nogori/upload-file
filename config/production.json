{
  "s3": {
    "bucket": {
      // Name of the bucket to use for file upload
      "name": "nogori_fileupload_production",
      // The number of seconds to expire the pre-signed URL operation in.
      "expire": 600
    }
  }
}